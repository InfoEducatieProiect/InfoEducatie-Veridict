import HeroSection from "@/components/hero-section"

/**
 * Veridict Landing Page
 * ─────────────────────
 * Shows the portal selection and login forms.
 * Authenticated users are redirected to their dashboards via middleware.
 */
export default function Page() {
  return <HeroSection />
}
